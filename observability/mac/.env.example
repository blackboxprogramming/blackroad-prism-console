# Copy to .env and update secrets before starting docker compose.
# If GF_INFLUX_TOKEN is left blank Grafana will start but dashboards
# will show "Datasource Error" until you paste a valid token.
GF_INFLUX_TOKEN=
