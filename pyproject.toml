[tool.black]
line-length = 100
target-version = ["py311"]

[tool.isort]
profile = "black"

[tool.ruff]
line-length = 100
extend-select = ["I"]

[tool.mypy]
python_version = "3.11"
packages = ["orchestrator", "bots", "config", "cli", "tools"]

[tool.pytest.ini_options]
pythonpath = ["."]
extend-select = ["I"]  # import sort
