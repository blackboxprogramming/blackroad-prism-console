# RevOps forecast check

Goal: RevOps forecast check.

Context:
```json
{}
```

CLI:
```
bot:run --bot "Demo-BOT" --goal "RevOps forecast check"
```

Artifacts:
- /artifacts/revops_forecast_check/output.txt
