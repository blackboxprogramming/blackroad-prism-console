1. Get the application URL by running these commands:
{{- if .Values.ingress.enabled }}
  kubectl get ingress {{ include "autopal.fullname" . }} -n {{ .Release.Namespace }}
{{- else }}
  kubectl get svc {{ include "autopal.fullname" . }} -n {{ .Release.Namespace }}
{{- end }}
