name: lucidia-review

on:
  push:
    branches: [main]
  pull_request:

jobs:
  review:
    runs-on: [self-hosted]
    steps:
      - uses: actions/checkout@v4
      - name: Run review
        run: |
          scripts/review.sh --ci
