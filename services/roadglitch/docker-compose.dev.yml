version: "3.9"
services:
  roadglitch:
    build: .
    ports:
      - "8080:8080"
    environment:
      ROADGLITCH_AUTH_TOKEN: dev-token
      ROADGLITCH_MOCK_MODE: "true"
    volumes:
      - ./data:/app/data
