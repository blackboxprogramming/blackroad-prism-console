name: Bug
description: Something not working
labels: [bug]
body:
  - type: textarea
    id: what
    attributes: { label: What happened? }
    validations: { required: true }
  - type: textarea
    id: repro
    attributes: { label: Steps to reproduce }
  - type: input
    id: url
    attributes: { label: URL / route (if site) }
name: 🐛 Bug report
description: Report a bug in the quantum app
labels: [bug]
body:
  - type: input
    id: url
    attributes: { label: Page URL, placeholder: "https://…" }
  - type: textarea
    id: what
    attributes: { label: What happened?, placeholder: Steps to reproduce, actual vs expected }
    validations: { required: true }
  - type: textarea
    id: logs
    attributes: { label: Console logs / screenshots }
  - type: dropdown
    id: browsers
    attributes: { label: Browsers, multiple: true, options: [Chrome, Firefox, Safari, Edge] }
