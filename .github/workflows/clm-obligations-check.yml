name: CLM Obligations Check
on:
  schedule: [{ cron: "0 7 * * *" }]
  workflow_dispatch:
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node -e "console.log('Obligations checked (stub)')"
