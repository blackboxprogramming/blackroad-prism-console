name: Marketing Monthly (ROAS & Campaign Report)
on:
  schedule: [{ cron: "25 6 1 * *" }]
  workflow_dispatch:
jobs:
  mkt-monthly:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: node scripts/mkt/roas.ts
