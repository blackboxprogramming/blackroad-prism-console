name: Privacy Daily (DSAR + DLP)
on:
  schedule: [{ cron: "15 2 * * *" }]
  workflow_dispatch:
jobs:
  privacy-daily:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955 # v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: node scripts/privacy/dsar_queue.ts
      - run: node scripts/privacy/dlp_rollup.ts
