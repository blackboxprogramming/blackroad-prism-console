name: SOX Quarterly (TOD/TOE + SoD)
on:
  schedule: [{ cron: "0 8 1 1,4,7,10 *" }]
  workflow_dispatch:
jobs:
  quarterly:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: node scripts/sox/sample_controls.ts
      - run: node scripts/sox/sod_scan.ts
