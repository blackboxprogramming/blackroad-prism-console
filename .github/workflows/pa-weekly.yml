name: Product Analytics Weekly (Funnels + Cohorts)
on:
  schedule: [{ cron: "10 13 * * 1" }]
  workflow_dispatch:
jobs:
  pa-weekly:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: node scripts/pa/funnel_report.ts
