name: Privacy Retention Purge
on:
  schedule: [{ cron: "20 1 * * *" }]
  workflow_dispatch:
jobs:
  purge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: node scripts/privacy/purge.js
