name: AI RAG Refresh
on:
  schedule: [{ cron: "0 4 * * *" }]
  workflow_dispatch:
jobs:
  rag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: node scripts/ai/rag_refresh.ts
