name: Warehouse ELT (Nightly)
on:
  schedule: [{ cron: "20 2 * * *" }]
  workflow_dispatch:
jobs:
  elt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: node scripts/warehouse/ingest.ts
      - run: node scripts/warehouse/stage.ts
      - run: node scripts/warehouse/model.ts
      - run: node scripts/warehouse/metrics_snapshot.ts
