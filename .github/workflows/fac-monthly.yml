name: Facilities Monthly (EHS Summary)
on:
  schedule: [{ cron: "20 8 1 * *" }]
  workflow_dispatch:
jobs:
  fac-monthly:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: node scripts/fac/ehs_report.ts
