name: CPQ Quote Expiry
on:
  schedule: [{ cron: "0 10 * * *" }]
  workflow_dispatch:
jobs:
  expire:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: node scripts/cpq/quote_expire.ts
