name: Patch Monthly (Rollup)
on:
  schedule: [{ cron: "45 3 1 * *" }]
  workflow_dispatch:
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: node scripts/patch/rollup.ts
