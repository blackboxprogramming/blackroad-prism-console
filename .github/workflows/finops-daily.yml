name: FinOps Daily (Ingest + Anomaly)
on:
  schedule: [{ cron: "0 4 * * *" }]
  workflow_dispatch:
jobs:
  finops-daily:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: node scripts/finops/ingest_csv.ts
      - run: node scripts/finops/anomaly.ts
