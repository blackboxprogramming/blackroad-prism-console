name: Auto Label
on:
  pull_request_target:
permissions: { pull-requests: write, contents: read }
jobs:
  label:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/labeler@v5
        with:
          configuration-path: .github/auto-labeler.yml
