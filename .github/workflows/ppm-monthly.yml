name: PPM Monthly (Roadmap)
on:
  schedule: [{ cron: "32 9 1 * *" }]
  workflow_dispatch:
jobs:
  ppm-monthly:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: node scripts/ppm/roadmap_report.ts
