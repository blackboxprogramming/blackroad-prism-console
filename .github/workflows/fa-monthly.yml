name: FA Monthly (Depreciation → GL Export)
on:
  schedule: [{ cron: "10 10 1 * *" }]
  workflow_dispatch:
jobs:
  fa:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: node scripts/fa/depr.ts
      - run: node scripts/fa/gl_export.ts
