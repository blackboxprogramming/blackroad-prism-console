name: WFM Monthly (Labor Cost)
on:
  schedule: [{ cron: "22 12 1 * *" }]
  workflow_dispatch:
jobs:
  wfm-monthly:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: node scripts/wfm/labor_cost.ts
