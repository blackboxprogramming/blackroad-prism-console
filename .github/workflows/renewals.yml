name: Renewals Forecast (Weekly)
on:
  schedule: [{ cron: "30 12 * * 1" }]
  workflow_dispatch:
jobs:
  renewals:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: npm ci
      - run: node scripts/renewal_forecast.js
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
          CRM_WAREHOUSE_URL: ${{ secrets.CRM_WAREHOUSE_URL }}
          CRM_WAREHOUSE_TOKEN: ${{ secrets.CRM_WAREHOUSE_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
