name: WFM Daily (Attendance Exceptions)
on:
  schedule: [{ cron: "12 12 * * *" }]
  workflow_dispatch:
jobs:
  wfm-daily:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: node scripts/wfm/attendance_exceptions.ts
