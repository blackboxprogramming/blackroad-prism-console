name: Support Monthly Report
on:
  schedule: [{ cron: "5 3 1 * *" }]
  workflow_dispatch:
jobs:
  report:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: node scripts/support/report.ts
      - uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore(support): monthly report"
          file_pattern: "support/reports/REP_*.md"
