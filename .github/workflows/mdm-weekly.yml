name: MDM Weekly (Survivorship & Dedupe Audit)
on:
  schedule: [{ cron: "25 4 * * 1" }]
  workflow_dispatch:
jobs:
  mdm-weekly:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: node scripts/mdm/audit.ts
