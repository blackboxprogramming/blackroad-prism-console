name: IAM Weekly (Access Digest)
on:
  schedule: [{ cron: "15 8 * * 1" }]
  workflow_dispatch:
jobs:
  iam-weekly:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: node scripts/iam/access_digest.ts
