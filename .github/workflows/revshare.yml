name: Partner RevShare (Monthly)
on:
  schedule: [{ cron: "15 2 1 * *" }]
  workflow_dispatch:
jobs:
  rev:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: node scripts/partner_revshare.ts
      - uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore(partner): monthly revshare report"
          file_pattern: "partner/reports/*.csv"
