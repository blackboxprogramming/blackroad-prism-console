name: SOX Annual (Mgmt Assessment + Pack)
on:
  schedule: [{ cron: "0 9 15 1 *" }]
  workflow_dispatch:
jobs:
  annual:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: node scripts/sox/management_assessment.ts
