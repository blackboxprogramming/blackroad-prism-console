name: Treasury Daily (Position + Payments Export)
on:
  schedule: [{ cron: "0 11 * * *" }]
  workflow_dispatch:
jobs:
  tre-daily:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: node scripts/tre/position.ts
