name: CPQ Approval Digest
on:
  schedule: [{ cron: "30 9 * * 1-5" }]
  workflow_dispatch:
jobs:
  digest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: node scripts/cpq/report.ts
