version: '3'
services:
  vault:
    image: hashicorp/vault:1.14.0
    cap_add:
      - IPC_LOCK
    environment:
      - VAULT_DEV_LISTEN_ADDRESS=0.0.0.0:8200
    volumes:
      - /srv/vault:/vault
    ports:
      - "8200:8200"
