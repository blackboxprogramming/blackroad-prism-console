[project]
name = "lmc-api"
version = "0.1.0"
description = "Lucidia Metadata & Catalog API service"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]"
]

[project.optional-dependencies]
dev = ["pytest"]
