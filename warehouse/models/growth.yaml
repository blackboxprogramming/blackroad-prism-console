models:
  - name: growth_funnel
    description: Signups → Activated → Paid
    sources: [ events_signups, events_activation, billing ]
    sql: |
      SELECT 'signup' as stage, 1000 as count
    owner: Growth
    sla_hours: 24
