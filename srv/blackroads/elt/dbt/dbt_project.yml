name: 'blackroads_dbt'
version: '1.0.0'
profile: 'blackroads'

model-paths: ['models']
seed-paths: ['seeds']

models:
  +schema: core
  staging:
    +schema: raw
    +materialized: view
  core:
    +materialized: table
  marts:
    +materialized: view

seeds:
  +schema: core
