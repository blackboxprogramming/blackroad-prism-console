name: 'blackroads_dbt'
version: '1.0.0'
# <!-- FILE: /srv/blackroads/elt/dbt/dbt_project.yml -->
name: 'blackroads'
version: '1.0'
config-version: 2
profile: 'blackroads'

model-paths: ['models']
seed-paths: ['seeds']

models:
  +schema: core
  staging:
    +schema: raw
    +materialized: view
  core:
    +materialized: table
  marts:
    +materialized: view

seeds:
  +schema: core
  blackroads:
    +schema: core
