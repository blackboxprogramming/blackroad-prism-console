{
  "name": "@blackroad/db",
  "version": "0.1.0",
  "description": "Database schema and seed utilities for BlackRoad licensing orchestration",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "schema.prisma", "seeds", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint src --ext .ts",
    "generate": "prisma generate"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "yaml": "^2.3.4"
  },
  "devDependencies": {
    "prisma": "^5.16.1",
    "typescript": "^5.5.4"
  }
}
