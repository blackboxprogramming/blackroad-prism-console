{
  "name": "@blackroad/worm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@blackroad/db": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.14.11",
    "rimraf": "^5.0.7",
    "typescript": "^5.4.5"
  }
}
