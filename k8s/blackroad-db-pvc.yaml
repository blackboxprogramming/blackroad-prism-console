apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: blackroad-db-pvc
  namespace: prism
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeName: blackroad-db-pv
