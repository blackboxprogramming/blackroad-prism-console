version: "3.9"
services:
  api:
    build: ../..
    command: pnpm --filter @blackroad/api dev
    ports: ["4000:4000"]
  proxy:
    image: nginx:alpine
    volumes: ["./nginx.conf:/etc/nginx/nginx.conf:ro"]
    ports: ["8080:80"]
