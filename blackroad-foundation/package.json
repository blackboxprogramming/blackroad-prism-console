{
  "name": "blackroad-foundation",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev:api": "pnpm --filter @blackroad/api dev",
    "build:all": "pnpm -r build",
    "lint": "eslint .",
    "format": "prettier -w .",
    "test": "node -e \"console.log('ok')\"",
    "deploy": "bash scripts/deploy_to_droplet.sh"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "prettier": "^3.2.0",
    "typescript": "^5.4.0"
  }
}
