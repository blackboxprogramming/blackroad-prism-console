global:
  scrape_interval: 15s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: 'codex-infinity'
    static_configs:
      - targets: ['api:8000']
  - job_name: 'nextjs-ui'
    static_configs:
      - targets: ['frontend:3000']
